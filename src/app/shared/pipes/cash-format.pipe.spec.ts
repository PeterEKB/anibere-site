import { CashFormatPipe } from './cash-format.pipe';

describe('CashFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CashFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
